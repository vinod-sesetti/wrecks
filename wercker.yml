box: python

build:
  steps:
    - virtualenv:
        name: setup virtual environment


box: python

build:
  steps:
   - pip-install:
        requirements_file: "requirements.txt"


box: python

build:
  steps:
    - script:
        name: Print
        code: print "hello"
